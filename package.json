{
  "name": "migro-hiring",
  "version": "1.0.0",
  "description": "Sistema de Contratación Migro - Frontend React",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:run": "vitest run",
    "test:ci": "vitest run --reporter=verbose",
    "docker:dev": "docker-compose up dev",
    "docker:prod": "docker-compose --profile production up prod",
    "docker:build": "docker-compose build",
    "docker:stop": "docker-compose down",
    "docker:clean": "docker-compose down -v --rmi all",
    "docker:logs": "docker-compose logs -f dev",
    "docker:shell": "docker-compose exec dev sh"
  }
}
